# 更新日志

本文档记录 TPMS Studio 各版本面向用户的功能变化。完整需求台账、技术决策、验证记录和已知限制见 [DEVELOPMENT_LOG.md](DEVELOPMENT_LOG.md)。

## [未发布]

### 新增

- 增加“仿真区域”视口切换，按入口、出口和壁面分别着色，并显示颜色图例。
- 仿真区域预览与“低质量单元”定位互斥，关闭后可恢复 TPMS 模型预览。
- 增加“自定义公式”建模类型，支持坐标变量、常用数学函数和 `min/max` 隐式实体组合。
- 选择“自定义公式”时自动隐藏无关的周期数量参数，切回内置 TPMS 后恢复显示。
- 模型生成完成后即可点击“网格质量”；首次查看会在临时目录后台生成 CFD 体网格质量数据，正式导出仍由“导出 COMSOL 流体网格”负责。
- 重新生成并替换 README 中的建模界面和 CFD 参数界面截图，使文档与当前版本一致。
- 扩展 README“界面预览”说明，补充工具栏、参数面板、三维视口、状态栏和 CFD 参数的逐项用途及典型操作流程。

### 改进

- 使用 `ui-ux-pro-max` 的专业工程软件规范重构主界面：参数控制由左侧栏迁移到顶部页签式工作台，并按“几何定义”“结构与孔隙率”“CFD 网格”分组。
- 将“生成模型”固定在参数工作台右上角，统计信息和 OpenGL 三维视口改为全宽布局，并补全输入焦点、禁用、悬停和按下状态。
- 更新建模界面和 CFD 网格页签截图，验证 `1120×720` 最小窗口下控件无重叠、文字无截断。
- 提高顶部工具栏、参数页签、表单标签、输入值和统计信息的字号与控件高度，改善高分辨率屏幕下的可读性。
- 再次优化顶部工作区的视觉层级：正文采用 16 px 基准字号，工具栏改为高对比深色工作栏，页签横向铺满，尺寸控件顶部对齐，并移除结构页无意义留白。
- 根据使用反馈将参数控制改回左侧可调栏，右侧恢复为独立的模型统计与 OpenGL 视口。
- 左侧几何、结构和 CFD 页签分别使用纵向参数流；长内容仅在参数栏内部滚动，“生成模型”固定在栏底部始终可用。

## [0.2.0] - 2026-09-17

### 新增

- 为 COMSOL 流体体网格增加真实棱柱边界层，支持设置首层高度、层数和增长率；核心流体域继续使用四面体单元填充。
- 增加入口/出口局部加密，可设置加密距离和局部单元尺寸比例。
- 增加基于曲率的自适应网格加密，可设置每圆周采样点数。
- 增加网格质量报告，统计缩放雅可比最小值、5% 分位数、中位数、平均值、质量直方图、最小单元体积和最大边长比。
- 增加低质量单元定位视图，可按自定义质量阈值在三维视口中高亮问题区域。
- 导出 `*_quality.vtu`，保存四面体和棱柱单元的 `scaled_jacobian` 质量场，便于在 ParaView 等工具中检查。
- COMSOL 网格导出支持混合 `CTETRA` 和 `CPENTA` 体单元。
- 增加过厚边界层导致高曲率或狭窄孔道自交时的明确错误提示。

### 改进

- 扩宽参数面板并移除横向滚动条，使 CFD 网格参数完整可见。
- 更新边界元数据，记录棱柱数量、体单元总数、边界层作用范围、质量统计和质量文件路径。
- 自动化测试扩展至 22 项，覆盖棱柱/四面体混合 BDF、质量 VTU 和网格质量元数据。

### 已知限制

- 棱柱边界层当前覆盖封闭流体域的全部边界，包括 TPMS 壁面、入口和出口；仅对 TPMS 壁面生成边界层仍需增加共形入出口分割。
- 体网格导出不会自动创建 COMSOL 材料、物理场、边界条件或求解器，正式仿真前仍需进行网格无关性检查。
- MSH/BDF 已通过本地回读验证，仍需在安装 COMSOL 的环境中完成最终导入验证。

## [0.1.0] - 2026-09-16

### 新增

- 提供 Gyroid、Diamond、Primitive、I-WP 和 Neovius 五类 TPMS 曲面。
- 支持片层结构和实体结构、三轴尺寸与周期、壁厚/等值面偏移和网格精度参数化控制。
- 支持按目标孔隙率自动反求结构参数，并显示实际孔隙率和求解误差。
- 使用 OpenGL GPU 三维预览，支持拖动旋转、滚轮缩放、重置视角以及暗色/亮色背景切换。
- 使用连通域清理去除游离网格碎片，并生成封闭三角网格。
- 支持 STL、OBJ 和 PLY 表面模型导出。
- 支持生成 TPMS 孔隙流体域，自动标记入口、出口、壁面和流体域，并导出 NASTRAN BDF、Gmsh MSH 与边界元数据。
